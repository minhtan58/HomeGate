#! /usr/bin/python3


class Devices(object):
    """docstring fo Devices."""

    def __init__(self,id,name,ieee,addr,type,model,manufacture,serial_number,sw_version,hw_version,signal,low_battery,info,created,updated):
        supe Devices, self).__init__()
        self.id = id
        self.ieee = ieee
        self.addr = addr
        self.type = generictype
        self.info = info
        self.model = model
        self.manufacture = manufacture
        self.serial_number = serial_number
        self.sw_version = sw_version
        self.hw_version = hw_version
        self.signal = signal
        self.low_battery = low_battery
        self.info = info
        self.created = created
        self.updated = updated
